<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent">

        <!-- Image outside dialog box -->

        <androidx.cardview.widget.CardView
            android:id="@+id/cardImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="100dp"
            android:elevation="5dp"
            android:layout_centerHorizontal="true">
            <ImageView
                android:id="@+id/ivImage"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:src="@drawable/ic_max_attempt"
                android:padding="15dp"
                android:elevation="10dp"
                android:scaleType="centerCrop"
                android:layout_marginTop="0dp" />
        </androidx.cardview.widget.CardView>


        <!-- Dialog box container -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/cardImage"
            android:layout_marginTop="-40dp"
            android:background="@drawable/grey_outline"
            android:backgroundTint="@color/white"
            android:paddingTop="50dp"
            android:paddingBottom="@dimen/twenty_dp"
            android:paddingHorizontal="@dimen/twenty_dp">

            <TextView
                android:id="@+id/tvMaxReach"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:fontFamily="@font/raleway_regular"
                android:textColor="@color/black"
                android:textSize="@dimen/eighteen_sp"
                android:text="You reached out maximum\namount of attempts.\nPlease, try later."
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnOkay"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Okay"
                android:textAllCaps="false"
                android:fontFamily="@font/raleway_regular"
                android:textColor="@color/white"
                app:cornerRadius="@dimen/thirty"
                android:paddingHorizontal="@dimen/thirty"
                android:backgroundTint="@color/black"
                app:layout_constraintTop_toBottomOf="@id/tvMaxReach"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginTop="@dimen/twenty_dp"/>
        </androidx.constraintlayout.widget.ConstraintLayout>

    </RelativeLayout>
</layout>
